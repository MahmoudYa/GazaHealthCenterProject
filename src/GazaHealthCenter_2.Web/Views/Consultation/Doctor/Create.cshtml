@using GazaHealthCenter_2.Objects.Models.Consultation
@model DoctorModel
@{
    var departments = ViewBag.Departments as List<DepartmentModel>;
}

<div class="container mt-4">
    <h2 class="mb-4">Add New Doctor</h2>

    <div class="card shadow p-4">
        <form asp-action="Create" method="post">
            <div class="mb-3">
                <label class="form-label">Doctor Name</label>
                <input type="text" asp-for="Name" class="form-control" placeholder="Enter doctor's name" required />
            </div>

            <div class="mb-3">
                <label class="form-label">Specialization</label>
                <input type="text" asp-for="Specialty" class="form-control" placeholder="Enter specialization" required />
            </div>

            <div class="mb-3">
                <label class="form-label">Department</label>
                <select asp-for="DepartmentId" class="form-select" required>
                    <option value="">Select Department</option>
                    @foreach (var department in departments)
                    {
                        <option value="@department.Id">@department.Name</option>
                    }
                </select>
            </div>

            <button type="submit" class="btn btn-success">✅ Add Doctor</button>
            <a asp-action="Index" class="btn btn-secondary">🔙 Back</a>
        </form>
    </div>
</div>
